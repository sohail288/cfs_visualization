"""
    git commit and push
    Start up an ec2 server.
    Install Dependencies
    Install virtualenv
    Start virtualenv
    Copy Config Files over
    NPM Install all the necessary things
    
"""

import boto3
import os
from os import path

def setup_deploy_environment():
    """
    Checks to see if environment contains the necessary files to proceed.
    Namely, a .boto file must be present in the home directory
    
    Return bool: 
    """
    pass 

    



if __name__ == "__main__":

    setup_deploy_environment()
